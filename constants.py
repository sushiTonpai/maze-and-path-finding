import pygame

# Define constants
WIDTH, HEIGHT = 800, 800
CELL_SIZE = 20
ROWS, COLS = HEIGHT // CELL_SIZE, WIDTH // CELL_SIZE

# Define colors
BLACK = (0, 0, 0)
WHITE = (255, 255, 255)
GREEN = (34, 139, 34)
BLUE = (0, 0, 255)
YELLOW = (255, 255, 0)
RED = (129, 0, 0)

# clock
clock = pygame.time.Clock()